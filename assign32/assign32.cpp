#include "assign32.h"

Piece :: Piece(bool isWhite) : isWhite(isWhite)
{
  // Make the default constructor work.
}

Piece :: ~Piece()
{
  // Destroy everything!
}

int main()
{
   Piece newPiece(0);
   
   
   
}
